VITE_APP_NAME=Eidolum
VITE_APP_URL=http://localhost:5173
# API base for the running app (absolute URL; preserves current dev behavior of
# hitting the deployed backend). Override locally if running a local backend.
VITE_API_BASE=https://api.eidolum.com
VITE_API_URL=http://localhost:8000
