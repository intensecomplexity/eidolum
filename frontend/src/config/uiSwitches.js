// Review-window switch (YouTube API audit III.E.4h, 2026-06-10): hides the
// /platforms pages, every link to them, and the profile "#N on <platform>"
// rank badge while the written reply is under review. Flip to true to
// restore everything in one line.
export const SHOW_PLATFORM_PAGES = false;
